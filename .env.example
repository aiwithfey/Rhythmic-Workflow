# Build with these set and the app runs against Postgres with real accounts.
# Build without them and it runs on seed data with no network — that is the
# design mockup, and how the published artifact is built.
#
# The publishable key is meant to be public; row level security is what
# protects the data, not the secrecy of this string.
SUPABASE_URL=https://puijleicxiiumkbbeect.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_2v6wxo4xpyX4Ubhwys8w-A_aXfRkgF6
